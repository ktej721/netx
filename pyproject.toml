[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "netx"
version = "0.1.0"
description = "Network Explorer CLI"
requires-python = ">=3.9"

[project.scripts]
netx = "netx.cli.main:main"
